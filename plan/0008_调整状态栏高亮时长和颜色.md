# 调整状态栏高亮时长和颜色

## 目标

- 新花费发生时，高亮持续时间改成 3 秒。
- 高亮颜色改成 `rgb(49, 235, 135)`，也就是 `#31EB87`。
- 保持只影响这个插件自己的状态栏项。

## 说明

- VS Code 状态栏支持给单个状态栏项设置前景色。
- 状态栏不支持字体加粗，所以不能真正做“粗体”。
- 本次实现采用更显眼的绿色前景色来替代加粗效果。

## 验证

- 编译通过。
- 新花费事件时，状态栏高亮持续约 3 秒。
