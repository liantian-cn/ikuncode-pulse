# 去掉弹出窗并改为状态栏高亮

## 目标

- 去掉轮询扣费时的弹出提示。
- 改为只用状态栏视觉变化提示：正常、刷新中、新花费高亮、错误。
- 保持 tooltip 里的详细信息不变。

## 改动

- 删除花费弹窗逻辑和对应设置项。
- 新增状态栏样式控制，统一设置文字、前景色、背景色。
- 新花费发生时，状态栏短暂高亮并显示本次新增金额。
- 错误时用错误色，配置缺失时用警告色。

## 验证

- 编译通过。
- 打包通过。
- 代码中不再有轮询扣费的弹窗提示调用。
