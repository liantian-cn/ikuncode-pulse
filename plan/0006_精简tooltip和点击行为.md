# 精简 tooltip 和点击行为

## 目标

- tooltip 底部不再显示任何按钮或命令链接。
- 点击状态栏时不再弹出菜单，改成直接刷新。
- 保持 tooltip 只负责展示信息，交互尽量简洁。

## 改动

- 删除状态栏菜单命令和对应代码。
- 状态栏点击命令改为刷新命令。
- 删除 tooltip 底部的命令链接文字。
- README 同步更新交互说明。

## 验证

- 编译通过。
- 打包通过。
- 点击状态栏时直接刷新，不再弹菜单。
